#' @param scenario (`list()`) \cr Data structure containing \pkg{irace}
#'   settings. The data structure has to be the one returned by the function
#'   [defaultScenario()] or [readScenario()].
#' <%=ifelse(exists("arg_appendix"), arg_appendix, "") %>

