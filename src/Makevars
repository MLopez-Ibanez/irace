# 							-*- mode: makefile -*-
EXE=
SOURCES = iracebin/irace.c
OBJECTS = $(SOURCES:.c=.o)
iraceEXE= iracebin/irace$(EXE)

all: $(iraceEXE)

$(iraceEXE): iracebin/irace.o
	$(CC) $(PKG_CPPFLAGS) $(CFLAGS)  $<  -o $@

clean:
	$(RM) -f iracebin/*.o $(iraceEXE)
