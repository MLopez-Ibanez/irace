# 							-*- mode: makefile -*-
SOURCES = iracebin/irace.c
OBJECTS = $(SOURCES:.c=.o)
EXE=

all: iracebin/irace$(EXE)

iracebin/irace$(EXE): iracebin/irace.o
	$(CC) $(PKG_CPPFLAGS) $(CFLAGS)  $<  -o $@

clean:
	$(RM) -f iracebin/*.o iracebin/irace$(EXE)



