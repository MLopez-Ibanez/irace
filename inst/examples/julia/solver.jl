# ------------------------------------------------------------------
# Your algorithm
# ------------------------------------------------------------------
using Random

function solve(instance, alpha, beta, iterations, seed)
    Random.seed!(seed)

    # Your real algorithm goes here.
    #
    # return objective_value

    return rand()
end
